/**
 * Texto en español de las páginas principales. Misma forma que copy.en.ts.
 * Cada sección que falte se reemplaza por la inglesa hasta que se escriba.
 */
import type { PageCopy } from './types';

export const copyEs: Partial<PageCopy> = {};
